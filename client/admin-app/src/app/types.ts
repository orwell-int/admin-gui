export type ServerGame = {
  serverGame: {
    name: string;
    up: boolean;
    address: string;
  };
};

export type ProxyRobots = {
  proxyRobots: {
    name: string;
    up: boolean;
    address: string;
  };
};
